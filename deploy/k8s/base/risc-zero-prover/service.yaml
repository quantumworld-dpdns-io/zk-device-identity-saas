apiVersion: v1
kind: Service
metadata:
  name: risc-zero-prover
  namespace: zk-identity
  labels:
    app.kubernetes.io/name: risc-zero-prover
    app.kubernetes.io/component: prover
spec:
  type: ClusterIP
  ports:
  - port: 3002
    targetPort: 3002
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/name: risc-zero-prover
    app.kubernetes.io/component: prover
