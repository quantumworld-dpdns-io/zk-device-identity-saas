apiVersion: v1
kind: Service
metadata:
  name: noir-prover
  namespace: zk-identity
  labels:
    app.kubernetes.io/name: noir-prover
    app.kubernetes.io/component: prover
spec:
  type: ClusterIP
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/name: noir-prover
    app.kubernetes.io/component: prover
